export class HistoricalData {
  constructor(public close: string, public high: string, public low: string,
   public open: string, public time: string, public volumefrom: string, public volumeto: string) {
  }
}