export class CryptoCompare {
  constructor(public body: string, public categories: string, public guid: string, public id: string,
              public imageurl: string, public lang: string, public published_on: string, 
              public source: string, public source_info: string[3], public tags: string, 
              public title: string, public url: string) {
  }
}